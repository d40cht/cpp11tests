

template<typename T>
void quickSort( std::vector<T>& data )
{
    int length = data.size();
    std::vector<T> buf( data.begin(), data.end() );
    
    auto pass = [length]( int from, int to, const std::vector<T>& from, std::vector<T>& to ) -> void
    {
    }
    
}


